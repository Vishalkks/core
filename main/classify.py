from core.main.Constants import directories

from core.main.Util.util import getObject

trainFreqs = getObject(directories.TRAIN_FREQS)
valFreqs = getObject(directories.VAL_FREQS)
testFreqs = getObject(directories.TEST_FREQS)

x=1